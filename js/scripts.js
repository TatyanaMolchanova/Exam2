$(document).ready(function(){
	
	// $('.slider').slick();

	$('.slider').slick({
	  arrows : false,
	  dots: true,
	});


	$('.carousel').slick({
	  arrows : true,
	  dots: true,
	  // autoplay: true,
  	//   autoplaySpeed: 4000,
	});



});